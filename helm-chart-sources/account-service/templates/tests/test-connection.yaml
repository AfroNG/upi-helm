apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upi-accountservice.fullname" . }}-test-connection"
  labels:
    {{- include "upi-accountservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upi-accountservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
